import { TransformInterceptorInterceptor } from './transform-interceptor.interceptor';

describe('TransformInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformInterceptorInterceptor()).toBeDefined();
  });
});
